# .env.example

# Database
DB_NAME=docscope
DB_USER=docscope_user
DB_PASSWORD=
DB_HOST=db
DB_PORT=5432

# App
SECRET_KEY=
DEBUG=true
LOG_LEVEL=INFO

# External documents drive (read-only bind mount into the api container)
# Absolute host path to the drive/folder holding the source documents.
DOCUMENTS_DIR=
